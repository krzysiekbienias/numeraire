# ============================================================================
# Numeraire++ — environment template
#
# Copy to `.env` (already gitignored) and adjust values for your local setup.
# Loaded at runtime by numeraire::utils::EnvLoader (Sprint 2+).
# Real env vars (`export FOO=bar`) override values from this file.
# ============================================================================

# --- Logging ----------------------------------------------------------------
NUMERAIRE_LOG_LEVEL=info               # trace | debug | info | warn | error | critical
NUMERAIRE_LOG_FILE=logs/numeraire.log  # rotating file sink target

# --- Database (Stage 2+) ----------------------------------------------------
# Path to the SQLite database that holds trade definitions.
# NUMERAIRE_DB_PATH=data/numeraire.sqlite

# --- Market data providers (Stage 3+) ---------------------------------------
# POLYGON_API_KEY=
# POLYGON_BASE_URL=https://api.polygon.io
